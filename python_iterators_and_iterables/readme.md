## Iterator and Iterable

- An iterator is an object which implements the iterator protocol, which consist of the methods __iter__() and __next__() 
- An iterable is an object whose values can be iterated.